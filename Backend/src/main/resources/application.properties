spring.application.name=demo
server.port = 8080 
# http://localhost:8080

# URL de conexion
spring.datasource.url=jdbc:mysql://localhost:3306/TravelAgency

# credenciales
spring.datasource.username=root
spring.datasource.password=clave1234

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Keycloak configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9090/realms/travelagency-realm

spring.security.oauth2.client.registration.keycloak.client-id=travelagency-backend
spring.security.oauth2.client.registration.keycloak.client-secret=08s0kH9En0SYKYuHNe4TPsvtl1qdPi0s
spring.security.oauth2.client.registration.keycloak.scope=openid,profile,email
spring.security.oauth2.client.registration.keycloak.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.keycloak.redirect-uri=http://localhost:9090/login/oauth2/code/keycloak
spring.security.oauth2.client.provider.keycloak.issuer-uri=http://localhost:9090/realms/travelagency-realm

# Check later if this is needed
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Config for data.sql
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
